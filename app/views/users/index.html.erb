<%= render 'books/usernew', books: @books %>

<h1>Users</h1>
<% @users.each do |user| %>
  <div>
    <p>image <%= image_tag book.user.get_profile_image(100,100) %></p>
    <p>name <%= user.name %></p>
    <%= link_to "Show", user_path(current_user.id) %>
  </div>
<% end %>