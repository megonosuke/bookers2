<h1>Users</h1>
<table class="table">
  <thead>
    <th>image</th>
    <th>name</th>
  </thead>

  <tbody>
    <% @users.each do |user| %>
    <tr>
      <td><!-- user.profile.imageを挿入したいがエラーが出るため一旦置いておく--></td>
      <td><%= user.name %></td>
      <td><%= link_to "Show", user_path(user.id) %></td>
    </tr>
    <% end %>
  </tbody>
</table>